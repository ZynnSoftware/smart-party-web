# Leave as "/api" to use the Vite dev proxy (works over the local network).
# Only set an absolute URL if you serve the frontend without the proxy.
VITE_API_URL=/api
# Clerk wired but disabled in MVP
VITE_CLERK_PUBLISHABLE_KEY=
